data_frame_one:
  columns:
    string:
      type: string
      length: 3
      pattern: "[ACGT]"
    integer:
      type: integer
      max: 20
    numeric:
      type: numeric
      min: 0
      max: 1
    bool:
      type: boolean
data_frame_two:
  columns:
    one:
      type: integer
    two:
      type: set
      set: ["a", "b", "c"]
  default_size: 5
