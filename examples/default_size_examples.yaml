size_not_set:
  columns:
    one:
      type: integer
    two:
      type: string
size_provided:
  columns:
    one:
      type: integer
    two:
      type: string
  default_size: 10
random_size:
  columns:
    one:
      type: integer
    two:
      type: string
  default_size:
    arguments:
      min: 1
      max: 3
random_static_size:
  columns:
    one:
      type: integer
    two:
      type: string
  default_size:
    arguments:
      min: 1
      max: 20
    static: TRUE
